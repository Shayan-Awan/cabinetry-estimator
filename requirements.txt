# Field Ticket-to-Invoice — local laptop application.
# Everything here runs offline. No cloud SDKs, no telemetry, no external APIs.

# --- Interface ---
streamlit>=1.40,<2

# --- Database ---
SQLModel>=0.0.22
SQLAlchemy>=2.0,<3

# --- Document handling (used from Phase 2 onward) ---
PyMuPDF>=1.24
Pillow>=10.4

# --- Extraction schemas ---
pydantic>=2.9,<3

# --- Local model access (plain HTTP to 127.0.0.1, used from Phase 2 onward) ---
httpx>=0.27

# --- Output documents (used from Phase 3 onward) ---
reportlab>=4.2
pypdf>=5.0

# --- Tests ---
pytest>=8.3
